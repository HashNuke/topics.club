name: CI

on:
  pull_request:
  push:
    branches: [main, app-split]

jobs:
  precommit:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ircpipe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ircpipe_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: erlef/setup-beam@v1
        with:
          otp-version: "28.5"
          elixir-version: "1.19.5"

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/ircpipe_web/assets/package-lock.json

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          npm ci --prefix apps/ircpipe_web/assets

      - name: Prevent boundary baseline expansion
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          if git cat-file -e "${BASE_SHA}:config/boundaries.exs" 2>/dev/null; then
            mix ircpipe.check_boundaries --baseline-ref "$BASE_SHA"
          else
            echo "Base commit predates the boundary manifest; running the head policy only."
            mix ircpipe.check_boundaries
          fi

      - name: Run precommit suite
        run: mix precommit

      - name: Build and inspect release artifacts
        env:
          IRCPIPE_SOURCE_REVISION: ${{ github.sha }}
        run: |
          set -euo pipefail

          MIX_ENV=prod mix assets.deploy
          MIX_ENV=prod mix release ircpipe
          MIX_ENV=prod mix release ircpipe_web
          MIX_ENV=prod mix release ircpipe_engine

          has_path() {
            compgen -G "$1" >/dev/null
          }

          combined=_build/prod/rel/ircpipe
          web=_build/prod/rel/ircpipe_web
          engine=_build/prod/rel/ircpipe_engine

          has_path "$combined/lib/ircpipe_core-*"
          has_path "$combined/lib/ircpipe_engine-*"
          has_path "$combined/lib/ircpipe_web-*"
          test -x "$combined/bin/server"
          test -x "$combined/bin/migrate"
          has_path "$combined/lib/ircpipe_web-*/priv/static/cache_manifest.json"

          has_path "$web/lib/ircpipe_core-*"
          has_path "$web/lib/ircpipe_web-*"
          ! has_path "$web/lib/ircpipe_engine-*"
          test -x "$web/bin/server"
          test -x "$web/bin/migrate"
          has_path "$web/lib/ircpipe_web-*/priv/static/cache_manifest.json"

          has_path "$engine/lib/ircpipe_core-*"
          has_path "$engine/lib/ircpipe_engine-*"
          ! has_path "$engine/lib/ircpipe_web-*"
          ! has_path "$engine/lib/ircpipe_core-*/ebin/Elixir.Ircpipe.Release.beam"
          test ! -e "$engine/bin/server"
          test ! -e "$engine/bin/migrate"
