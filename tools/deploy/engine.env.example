# Copy this list to /etc/topics-club/engine.env on the destination host.
# Replace every placeholder there. Never commit the resulting file.
DATABASE_URL=replace-on-host
IRC_CREDENTIALS_KEY=replace-on-host
POOL_SIZE=10
DB_QUEUE_TARGET=5000
DB_QUEUE_INTERVAL=5000
RELEASE_NODE=topics_club_engine@localhost
RELEASE_COOKIE=replace-on-host
