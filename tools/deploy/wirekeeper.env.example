# Copy this list to /etc/topics-club/wirekeeper.env on the destination host.
# Replace every placeholder there. Never commit the resulting file.
RELEASE_NODE=topics_club_wirekeeper@localhost
RELEASE_COOKIE=replace-on-host
